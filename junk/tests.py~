import sys

def test_ai_strategies(ai,x,y):
    # Tests wether ai updated its strategies correctly
    for strategy in ai.defence_strategies:
        if (x,y) in strategy:
            print("Error in ai strategies")
            print("This shouldn't be here: "+  str((x,y)))
            sys.exit()
